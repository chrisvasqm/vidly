@model List<Movie>

@{
    ViewBag.Title = "Movies";
    Layout = "_Layout";
}

<h2>@ViewBag.Title</h2>

@if (Model.Count == 0)
{
    <p>There are no movies left.</p>
}
else
{
    <table class="table table-striped table-hover ">
        <thead>
        <tr>
            <th>Name</th>
            <th>Genre</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var movie in @Model)
        {
            <tr>
                <td>@movie.Name</td>
                <td>@movie.Genre.Name</td>
            </tr>
        }
        </tbody>
    </table>
}