@model Vidly.ViewModel.CustomerFormViewModel

@{
    ViewBag.Title = "New";
    Layout = "_Layout";
}

<h2>@Model.Title</h2>

@using (@Html.BeginForm("Save", "Customer"))
{
    @Html.ValidationSummary()
    <div class="form-group row">
        <div class="col-xs-3">
            @Html.LabelFor(m => m.Name)
            @Html.TextBoxFor(m => m.Name, new {@class = "form-control"})
            @Html.ValidationMessageFor(m => m.Name)
        </div>
    </div>
    <div class="form-group row">
        <div class="col-xs-3">
            @Html.LabelFor(m => m.MembershipTypeId)
            @Html.DropDownListFor(m => m.MembershipTypeId, new SelectList(Model.MembershipTypes, "Id", "Name"), "Select a Membership Type", new {@class = "form-control"})
        </div>
    </div>
    <div class="form-group row">
        <div class="col-xs-3">
            @Html.LabelFor(m => m.Birthday)
            @Html.TextBoxFor(m => m.Birthday, "{0:d MM yyyy}" , new {@class = "form-control"})
        </div>
    </div>
    <div class="checkbox">
        <label>
            @Html.CheckBoxFor(m => m.IsSubscribedToNewsLetter) Subscribe to Newsletter?
        </label>
    </div>
    @Html.HiddenFor(m => m.Id);
    @Html.AntiForgeryToken()
    <button type="submit" class="btn btn-primary">Save</button>
}